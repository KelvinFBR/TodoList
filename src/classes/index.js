// Un buen truco es crear un archivo aparerte al cual vamos hacer todas las importaciones y luego colocamos una unica importacion al archivo de la aplicacion esto para que sea mas organizado

// todo: Aqui todas las importaciones para luego solo  hacer una una importacion.
import { TodoList } from "./todo-list.class";
import { Todo } from "./todo.class";

export { Todo, TodoList };
